import React from 'react';
import { Box, Typography } from '@mui/material';

export const QueryEditor = () => {
  return (
    <Box>
      <Typography>Welcome to the Query editor</Typography>
    </Box>
  );
};
